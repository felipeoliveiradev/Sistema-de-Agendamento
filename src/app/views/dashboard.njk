{% extends "_layouts/default.njk" %}


{% block body %}
<div class="dashboard">
<div class="left" style="background-color:#000">
      <img src="/files/{{user.avatar}}"/>
      <h1>Olá {{user.name}}</h1>
      <hr style="border-color:#fff" />

</div>

<div class="right">
    <h1>Produtores Cadastrados com Agente</h1>

    {% for provider in providers %}
        <li class="provider">
            <div class="foto"><img src="/files/{{ provider.avatar }}"/></div>
            <div class="provider">
                <strong>{{ provider.name }}</strong>
            </div>
            <div class="botao">
                <a href="">Reservar</a>
            </div>
        </li>
    {% endfor %}

</div>
</div>

{% endblock %}
